package tekgui.window.layout;


/**
 * A layout that places the objects in the outermost ring first, 
 * then as each ring fills, adds them into an inner ring.  
 * Objects are not touching and will rearrange when connections are 
 * on?
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class SpiralLayout{
    
}
